<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Phantom Guard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-dark text-light">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom border-secondary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/img/logo.png" alt="Phantom Guard Logo" width="32" height="32" class="d-inline-block align-text-top me-2">
                Phantom Guard
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="bi bi-house-fill"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://discord.com/oauth2/authorize?client_id=<%= client.user.id %>&permissions=8&scope=bot%20applications.commands" target="_blank">
                            <i class="bi bi-plus-circle-fill"></i> Add to Server
                        </a>
                    </li>
                </ul>
                <div class="d-flex">
                    <div class="dropdown">
                        <button class="btn btn-dark dropdown-toggle" type="button" data-bs-toggle="dropdown">
                            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="<%= user.username %>" 
                                width="32" height="32" class="rounded-circle me-2">
                            <%= user.username %>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-dark">
                            <li><a class="dropdown-item" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout"><i class="bi bi-box-arrow-right"></i> Logout</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container py-5">
        <h1 class="mb-4">Select a Server</h1>
        
        <% if (guilds && guilds.length > 0) { %>
            <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-4">
                <% guilds.forEach(guild => { %>
                    <div class="col">
                        <div class="card h-100 bg-dark border-secondary">
                            <div class="d-flex align-items-center p-3">
                                <% if (guild.icon) { %>
                                    <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" 
                                        alt="<%= guild.name %>" class="rounded-circle me-3" width="64" height="64">
                                <% } else { %>
                                    <div class="rounded-circle bg-secondary me-3 d-flex align-items-center justify-content-center" 
                                        style="width: 64px; height: 64px;">
                                        <span class="fw-bold text-light fs-4"><%= guild.name.charAt(0) %></span>
                                    </div>
                                <% } %>
                                <div>
                                    <h5 class="card-title mb-1"><%= guild.name %></h5>
                                    <p class="card-text small text-muted mb-0">ID: <%= guild.id %></p>
                                </div>
                            </div>
                            <div class="card-footer bg-dark d-grid">
                                <a href="/dashboard/<%= guild.id %>" class="btn btn-primary">
                                    <i class="bi bi-gear-fill"></i> Manage Server
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="alert alert-info">
                <i class="bi bi-info-circle-fill me-2"></i>
                You don't have any servers with Phantom Guard added, or you don't have the necessary permissions.
            </div>
            <p>To use the dashboard, you need:</p>
            <ul>
                <li>A Discord server where you have administrator permissions</li>
                <li>Phantom Guard added to that server</li>
            </ul>
            <a href="https://discord.com/oauth2/authorize?client_id=<%= client.user.id %>&permissions=8&scope=bot%20applications.commands" 
                class="btn btn-primary mt-3" target="_blank">
                <i class="bi bi-plus-circle-fill"></i> Add Phantom Guard to a Server
            </a>
        <% } %>
    </div>

    <!-- Footer -->
    <footer class="py-4 bg-dark border-top border-secondary mt-auto">
        <div class="container text-center">
            <p class="mb-0">&copy; 2025 Phantom Guard. All rights reserved.</p>
            <p class="small text-muted">Not affiliated with Discord or YouTube.</p>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>